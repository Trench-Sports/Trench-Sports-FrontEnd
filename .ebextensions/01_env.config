# Ensures Elastic Beanstalk installs devDependencies during deployment
# so Vite can build the frontend on the instance.
# See: NPM_USE_PRODUCTION in the EB Node.js dependencies docs.
option_settings:
  aws:elasticbeanstalk:application:environment:
    NPM_USE_PRODUCTION: "false"
